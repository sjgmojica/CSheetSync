/**
 * ============================================================
 * CSV Import Logger
 * ============================================================
 */

function csvLogImport(summary) {
  Logger.log(summary);
}